# Caliper

To build this project with Maven:

```sh
cd caliper
mvn eclipse:configure-workspace eclipse:eclipse install
```

To build examples:

```sh
cd examples
mvn eclipse:configure-workspace eclipse:eclipse install
```
